---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: next-deploy-project-d840b523                  # Ingress name
  labels:
    app: next-deploy-project-d840b523
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "next-deploy-project-d840b523.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: next-deploy-project-d840b523               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
